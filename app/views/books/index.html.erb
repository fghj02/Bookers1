<div>
  <%= flash[:notice] %>
</div>

<table>
<h1>Your Books</h1>

<% @books.each do |book| %>

<tr>
<th>Title</th>
<td><span><%= book.title %></spam></td>

<th>Body</th>
<td><span><%= book.body %></span></td>
<td><%= link_to "Show", book_path(book.id) %></td>
<td><%= link_to "Edit", edit_book_path(book.id) %></td>
<td><%= link_to "Destroy", book_path(book.id), method: :delete, "data-confirm" => "Are you ok？" %></td>
<% end %>
</tr>
</table>



<h1>New book</h1>

<% if @book.errors.any? %>
  <%= @book.errors.count %>error(s) prohibited this book form being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with model: @book, url: '/books', method: :post do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>
<% end %>

